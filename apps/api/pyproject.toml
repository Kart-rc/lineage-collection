[project]
name = "lineage-collector-api"
version = "0.1.0"
description = "Local lineage collector walking-skeleton API"
requires-python = ">=3.12,<3.14"
dependencies = [
  "fastapi>=0.116,<0.117",
  "jsonschema>=4.25,<5",
  "uvicorn>=0.35,<0.36",
]

[project.optional-dependencies]
aws = [
  "boto3>=1.40,<2",
]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.4,<9",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lineage_api"]

[tool.pytest.ini_options]
addopts = "--strict-config --strict-markers"
pythonpath = ["src"]
testpaths = ["tests", "../../tests"]
